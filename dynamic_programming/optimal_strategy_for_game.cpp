#include<iostream>
#include<vector>
using namespace std;

void optimalGame(int start,int end,vector<int>& coins,bool flag,int& ans){
    if(end<start){return;}
    if(flag){
        
    }
    return ;
    
}

int main(){
    vector<int>coins{20,5,4,6};
    int ans=0
    optimalGame(0,coins.size()-1,coins,true,ans);
    return 0;
}